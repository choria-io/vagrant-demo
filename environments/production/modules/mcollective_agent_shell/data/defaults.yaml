lookup_options:
  mcollective_agent_shell::gem_dependencies:
    merge:
      strategy: deep
  mcollective_agent_shell::package_dependencies:
    merge:
      strategy: deep
  mcollective_agent_shell::config:
    merge:
      strategy: deep
  mcollective_agent_shell::client_config:
    merge:
      strategy: deep
  mcollective_agent_shell::server_config:
    merge:
      strategy: deep
